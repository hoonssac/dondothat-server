<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.bbagisix.common.codef.mapper.CodefAccessTokenMapper">

    <select id="getCurrentToken" resultType="org.bbagisix.common.codef.domain.CodefAccessTokenVO">
        select token_id     as tokenId,
               access_token as accessToken,
               expires_at   as expiresAt,
               created_at   as createdAt,
               updated_at   as updatedAt
        from access_token
        order by created_at desc limit 1
    </select>

    <insert id="insertToken" parameterType="org.bbagisix.common.codef.domain.CodefAccessTokenVO">
        insert into access_token (access_token, expires_at)
        values (#{accessToken}, #{expiresAt})
    </insert>


    <update id="updateToken">
        update access_token
        set access_token = #{accessToken},
            expires_at   = #{expiresAt}
        where token_id = #{tokenId}
    </update>
</mapper>